<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Jika file/folder benar-benar ada, langsung serve
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Redirect semua request ke index.html (SPA routing)
  RewriteRule . /index.html [L]
</IfModule>
